services:
  db:
    container_name: db
    image: postgres:latest
    env_file:
      - ./.env
    ports:
      - 5432:5432
    networks:
      - my-pg-network
networks:
  my-pg-network:
    driver: bridge
